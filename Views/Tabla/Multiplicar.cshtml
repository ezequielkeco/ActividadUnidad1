@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@{
    ViewData["Title"] = "Tabla de Multiplicar";
    var numero = (int)ViewBag.Numero;
    var resultados = ViewBag.Resultados as List<string>;
}

El número digitado es: @numero - A continuación, se muestra su tabla de multiplicar.

<ul>
    @foreach (var linea in resultados)
    {
        <li>@linea</li>
    }
</ul>
